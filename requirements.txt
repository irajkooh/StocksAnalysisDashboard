# ── Core ──────────────────────────────────────────────────────────────────────
# Packages below marked "local only" are injected by HF Spaces — uncomment when installing locally.
gradio>=5.25.0
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0

# ── LangGraph ─────────────────────────────────────────────────────────────────
langgraph>=1.1.3
langchain>=1.2.12
langchain-core>=1.2.20
langchain-community>=0.4.1
# langchain-ollama>=1.0.0   # local only — Ollama does not run on HF Spaces

# ── LLM ───────────────────────────────────────────────────────────────────────
groq>=1.1.1

# ── Market data ───────────────────────────────────────────────────────────────
yfinance>=1.2.0
pandas>=2.0.0
numpy>=2.0.0

# ── Charts ────────────────────────────────────────────────────────────────────
plotly>=6.6.0
mplfinance>=0.12.10b0
matplotlib>=3.10.0
Pillow>=8.0.0

# ── Sentiment ─────────────────────────────────────────────────────────────────
requests>=2.32.5
praw>=7.8.0
newsapi-python>=0.2.7
beautifulsoup4>=4.14.3

# ── TTS ───────────────────────────────────────────────────────────────────────
gTTS>=2.5.4
audioop-lts>=0.2.1; python_version >= "3.13"    # pydub compat shim — audioop was removed in Python 3.13

# ── MCP Server ────────────────────────────────────────────────────────────────
# mcp[cli]>=1.27.0         # local only — only needed to run capitol_trades_mcp.py as MCP server

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.2.2
httpx>=0.28.1
aiofiles>=24.1.0
APScheduler>=3.10.0
