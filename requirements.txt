# NOTE: fastapi and gradio are injected by HF Spaces (fastapi<0.113.0, gradio==4.31.0)

# Commenting them out here avoids the build conflict; uncomment for local installs.
# fastapi>=0.100.0
# gradio>=4.0.0
uvicorn>=0.22.0
httpx>=0.24.0
yfinance>=0.2.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.0.0
ta>=0.11.0
pytz>=2023.3
